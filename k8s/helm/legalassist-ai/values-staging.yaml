# Staging Environment Values
replicaCount: 2

image:
  repository: ghcr.io/yourusername/legalassist-ai
  tag: staging
  pullPolicy: Always

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

ingress:
  enabled: true
  hosts:
    - host: staging.legalassist.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: staging-tls
      hosts:
        - staging.legalassist.example.com

postgresql:
  auth:
    password: ${POSTGRES_PASSWORD:-CHANGE-ME-STAGING}
  primary:
    persistence:
      size: 10Gi

redis:
  auth:
    password: ${REDIS_PASSWORD:-CHANGE-ME-STAGING}

env:
  - name: ENVIRONMENT
    value: staging
  - name: LOG_LEVEL
    value: DEBUG
  - name: ENABLE_PROFILING
    value: "true"
